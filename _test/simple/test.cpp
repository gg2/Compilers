// This is a comment

%:include    "incl.h"
#define bullshit (~)

int funcBlank1( char, int* );

int main( int argc, char **argv )
{
	char *_abcdefghijklmnopqrstuvwyxy;
	float big_un=-1234567890;
	
	if ( honky and bullshit )
		printf( "How big is the big_un? %g\n", big_un );
	else if ( honky && bullshit )
		printf( "How small is the big_un? %g\n", big_un );
	else
		honk it;
		
	return true;
}