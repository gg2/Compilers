CS121
Lab11
Gabe Gibler

TV Show Database

Data Structures
A) Binary Search Tree
B) Linked List
C) BS Tree Node data:
	1) Title
	2) Genre
	3) Opening Year
	4) Ending Year
	5) Linked List( actors )
	6) Source

BinarySearchTree( TV Show Data )
Get filename from command line
Parse file and store data
	Fill in TV Show object properties, incl.
	actors to linked list
	save object to tree
Run through target operations
	Get any required input per operation
	Run assoc function

Functions
*) List show titles()
	InOrder, for each {
		add title to list
	}
	return: list of titles
*) List actors per show( title )
	PreOrder, for each {
		if title matches {
				fetch list of actors
		}
	}
	return: list of actors
*) List shows per actor( actor )
	PreOrder, for each {
		for each actor, if actor matches {
			add title to list
		}
	}
	sort list
	return: list of titles
*) List shows per timeframe( year1, year2 )
	PreOrder, for each {
		if opening >= year1 AND opening <= year2 {
			add title to list
		}
	}
	sort list
	return: list of titles
*) Sort list( list )
	alphabetically